pipeline {
    agent any

    stages {
        stage('Create directories') {
            steps {
                dir('/jenkins/uperdata/scritps') {
                    chmod +x create_directories_hdfs.sh
                    echo "funciono"
                }
            }
        }
    }
}