pipeline {
    agent any

    stages {
        stage('Run') {
            steps {
                dir('/pipelines/cleandata') {
                    sh 'python3 main.py'
                }
            }
        }
    }
}
